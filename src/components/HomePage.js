import Form from "./form";

import { useNavigate } from "react-router-dom";
function HomePage() {
  //const history = useNavigate();

  //   const handleClick = () => {
  //     history.push("/second"); // Navigate to the '/second' path
  // };

  return (
    <div>
      <Form />
    </div>
  );
}

export default HomePage;
